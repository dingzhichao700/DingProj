var egret;
(function (egret) {
    var ElementCollectGoods = (function (_super) {
        __extends(ElementCollectGoods, _super);
        /**
         * 构造函数
         */
        function ElementCollectGoods() {
            _super.call(this);
            //特效
            this._effect = null;
        }
        var __egretProto__ = ElementCollectGoods.prototype;
        /**
         * 添加至场景时处理
         *
         */
        __egretProto__.addToScene = function () {
            _super.prototype.addToScene.call(this);
            this.play(0, egret.ActionType.PREPARE, egret.ActionMovieClipDirectionType.UP);
            //特效动画
            //if(!this._effect){
            //	var loader:Loader = ResoureManager.getInstance().getResourceLoader(ResourceId.ID_RESOURCE_COLLECT_EFFECT);
            //	if(loader){
            //		this.loadEffectComplete();
            //	}else{
            //		ResoureManager.getInstance().load([ResourceId.ID_RESOURCE_COLLECT_EFFECT],this.loadEffectComplete,null,null,true);
            //	}
            //}else{
            //	this._effect.gotoAndPlay(1);
            //}
        };
        //
        __egretProto__.removeFromScene = function () {
            _super.prototype.removeFromScene.call(this);
            this._effect.stop();
        };
        /**
         * 加载特效完成
         *
         */
        //private loadEffectComplete(item:LoadDataItem = null):void{
        //	var loader:Loader = ResoureManager.getInstance().getResourceLoader(ResourceId.ID_RESOURCE_COLLECT_EFFECT);
        //	ResoureManager.getInstance().loadBytes(loader.contentLoaderInfo.bytes,this.loadComplete);
        //}
        /**
         * 内存加载特效
         * @param loader
         *
         */
        //private loadComplete(loader:Loader,...args):void{
        //	this._effect = <MovieClip><any> (loader.content);
        //	this.addChild(this._effect);
        //
        //	this._effect.x = -this._effect.width / 2;
        //	this._effect.y = -this._effect.height / 2;
        //
        //	this._effect.gotoAndPlay(1);
        //}
        //
        /**
         * 获取部件影片地址
         * @param partType:String ActionPartType 动作影片类型
         * @return
         */
        __egretProto__.getPartUrl = function (partType) {
            return _super.prototype.getPartUrl.call(this, partType);
            //var path:string = PathData.PATH_MOVIES_COLLECT_GOODS;
            //
            //var movie:string = this._sceneAvatarVo[partType];
            //
            //var url:string = dataManager().sceneElementData.getActionUrl(path,movie,this._avatar.actionType);
            //
            //return url;
        };
        return ElementCollectGoods;
    })(egret.SceneElementInteractive);
    egret.ElementCollectGoods = ElementCollectGoods;
    ElementCollectGoods.prototype.__class__ = "egret.ElementCollectGoods";
})(egret || (egret = {}));
//# sourceMappingURL=ElementCollectGoods.js.map