
module egret {

	export class BaseVo{
        /**
        * 返回自身不包括包路径的类名，ProtocolVo 实例的名称:"ProtocolVo"，
		 * 因egret发布后使用混淆技术，类名全被改变，此值手动设置
        */
        public className: string;
		/**
		 * 构造函数
		 */
		public constructor(){
			//this.className = this.constructor["name"];
		}
	}
}