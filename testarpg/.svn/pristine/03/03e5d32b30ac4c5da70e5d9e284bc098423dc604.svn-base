module egret{
    /**
     * 因egret的主程序与UI层分开，所以需要两个应用程序，此类为主程序
     * @see ApplicationUI
     */
    export class ApplicationMain extends Application{
        //
        public constructor(){
            super(true);
        }
        //
        public initWindow():void{
            super.initWindow();
        }
    }
}
